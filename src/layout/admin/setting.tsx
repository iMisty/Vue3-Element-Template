import { Component, Vue } from 'vue-property-decorator';

@Component({})
export default class AdminSetting extends Vue {
  private render() {
    return <div class="admin__setting">setting</div>;
  }
}
